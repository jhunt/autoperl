AUTOMAKE_OPTIONS = foreign subdir-objects
EXTRA_DIST =

bin_PROGRAMS  =
bin_PROGRAMS += util
util_SOURCES = src/util.c

PERL_TESTS  =
PERL_TESTS += t/00-perl.t

TESTS = $(PERL_TESTS)

EXTRA_DIST += $(PERL_TESTS)
EXTRA_DIST += t/common.pm
EXTRA_DIST += t/utility
EXTRA_DIST += helper.pl
